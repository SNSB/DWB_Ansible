[defaults]
inventory      = inventory/hosts.ini
roles_path     = roles
host_key_checking = False
# Optional: feste Passwortdatei statt --ask-vault-pass bei jedem Lauf.
# vault_password_file = ~/.ansible/vault_pass.txt
